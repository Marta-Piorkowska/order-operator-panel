import { Box, Typography } from "@mui/material";

export const OrdersPage = () => {
   return (
      <Box sx={{ p: 4 }}>
         <Typography variant="h4">
            Zamówienia
         </Typography>
      </Box>
   );
};
