import { Box, Typography } from "@mui/material";

export const LoginPage = () => {
   return (
      <Box sx={{ p: 4 }}>
         <Typography variant="h4">
            Logowanie
         </Typography>
      </Box>
   );
};
